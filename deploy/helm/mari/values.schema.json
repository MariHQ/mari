{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "api": {
      "type": "object",
      "properties": {
        "replicaCount": {
          "type": "integer",
          "const": 1,
          "description": "Mari v0.1.2 uses one API replica with its ReadWriteOnce data volume."
        },
        "persistence": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "const": true,
              "description": "The self-contained v0.1.2 chart always persists application data."
            }
          }
        }
      }
    },
    "web": {
      "type": "object",
      "properties": {
        "replicaCount": {
          "type": "integer",
          "const": 1,
          "description": "Mari v0.1.2 supports exactly one web replica."
        }
      }
    },
    "secrets": {
      "type": "object",
      "properties": {
        "existingSecret": {
          "type": "string",
          "description": "Recommended: name of a Secret containing only POSTGRES_PASSWORD and MARI_DB."
        },
        "databasePassword": {
          "type": "string",
          "pattern": "^[A-Za-z0-9._~-]*$",
          "description": "Required when existingSecret is empty. Use a long URL-safe value; never commit it."
        }
      }
    },
    "postgresql": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "const": true,
          "description": "The self-contained v0.1.2 chart always deploys PostgreSQL/pgvector."
        }
      }
    },
    "ingress": {
      "type": "object",
      "properties": {
        "enabled": {"type": "boolean"},
        "className": {"type": "string"},
        "host": {
          "type": "string",
          "description": "Optional customer-owned DNS hostname. Blank creates a hostless ingress."
        }
      }
    }
  }
}
