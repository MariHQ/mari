# Exact pins match server/.venv (pip freeze, 2026-09-02). Bump deliberately,
# with the venv, so the image and local runs resolve the same set.
fastapi==0.139.2
strawberry-graphql[fastapi]==0.322.1
uvicorn[standard]==0.51.0
psycopg[binary]==3.3.4
psycopg_pool==3.3.1
nh3==0.3.6
markdown==3.10.2
boto3==1.43.52
pydantic==2.13.4
PyJWT[crypto]>=2.10,<3
numpy>=2.0
duckdb>=1.3
pyiceberg[pyarrow]>=0.10
sqlalchemy>=2.0
httpx>=0.28,<1
slack-sdk>=3.36,<4
